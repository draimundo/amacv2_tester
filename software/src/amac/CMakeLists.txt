SET(SrcFiles AMACTB.cpp
)

ADD_LIBRARY(amac SHARED ${SrcFiles}) 
INSTALL(TARGETS amac LIBRARY DESTINATION lib)
SET_TARGET_PROPERTIES(amac PROPERTIES
  SOVERSION ${amac_VERSION_MAJOR}
  VERSION ${amac_VERSION_MAJOR}.${amac_VERSION_MINOR}
  )
